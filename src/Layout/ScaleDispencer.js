import React from 'react'

const formationToClass = (formation) => {
    if ( formation.type == 'stream') {
        const className = 'btn' //Foundation
    }
    else if ( formation.type == 'course' ) {}
}
