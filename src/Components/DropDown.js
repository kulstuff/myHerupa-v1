import React from 'react'

export default function DropDown({props}) {
    var timesRender = props.number
    switch(props.dropAction) {
        case "Year":
            const yearList = ["First" , "Second"];
    }
    return (
        <div>
            {
                
            }
        </div>
    )
}
